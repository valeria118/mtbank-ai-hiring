fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
faster-whisper==1.1.1
openai==1.51.0
httpx==0.27.2
python-dotenv==1.0.1
pydub==0.25.1
# audioop удалён из стандартной библиотеки в 3.13 — pydub его импортирует.
audioop-lts==0.2.1; python_version >= "3.13"
# numpy 2.4.6, не 1.26.4 и не более новый 2.5.x:
# - pyannote-core (транзитивная зависимость pyannote.audio, см. ниже)
#   требует numpy>=2.0 — с 1.26.4 `pip install` падает с ResolutionImpossible
#   (обнаружено при живой сборке Docker-образа pipelines).
# - numpy>=2.5.0 требует Python>=3.12, а базовый образ pipelines
#   (ghcr.io/open-webui/pipelines:main) — на Python 3.11: с 2.5.x pip падает
#   с "No matching distribution found" (тоже обнаружено при живой сборке).
# 2.4.6 — последняя стабильная версия, совместимая и с тем, и с другим.
numpy==2.4.6
jiwer==3.0.4
tenacity==9.0.0
requests==2.32.3
prometheus-client==0.21.0
edge-tts==7.2.8

# Диаризация по голосу (опционально, см. DIARIZATION_BACKEND в .env.example).
# torchaudio больше не развивается отдельно от torch (последний релиз — 2.11.0,
# torch тем временем ушёл на более новые версии) — версии закреплены строго
# парой, иначе pip тянет несовместимые torch/torchaudio и pyannote падает на
# импорте (AttributeError: module 'torchaudio' has no attribute 'AudioMetaData').

# torch==2.11.0
# torchaudio==2.11.0

# torchvision мы напрямую не используем, но образ ghcr.io/open-webui/pipelines
# уже содержит его (собранный под torch==2.8.0, под примеры пайплайнов из
# коробки OpenWebUI) — при обновлении torch до 2.11.0 старый torchvision
# рассинхронизируется и падает с "partially initialized module 'torchvision'
# has no attribute 'extension' (circular import)", как только что-то в цепочке
# импортов pyannote его затрагивает (обнаружено при живом запуске). Явно
# закрепляем версию, требующую ровно torch==2.11.0, чтобы pip поставил
# совместимую, а не оставил старую от базового образа.

# torchvision==0.26.0
# pyannote.audio==4.0.7

# тесты
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
